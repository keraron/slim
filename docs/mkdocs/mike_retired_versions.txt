# Mike version identifiers to remove from gh-pages (one per line).
# The delete script and CI run `mike delete` for each so they disappear from the site dropdown.
# Remove a line when you no longer need to delete that version (e.g. already gone).

1.0.0
1.0.1
1.0.2
