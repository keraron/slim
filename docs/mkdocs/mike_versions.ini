# Used by docs/mike_version.py and `task docs:mike:*`.
#
# local   — `task docs:mike:deploy-local` (local gh-pages, no push)
# release — `task docs:mike:deploy` when VERSION is unset
#
# Published doc versions on gh-pages: deploy 1.3.0 and 1.3.1 so both appear in the
# Material version selector (see docs/README.md and publish-versioned-docs.sh).

[versions]
local = 1.3.1
release = 1.3.1
