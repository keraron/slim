# SPDX-FileCopyrightText: Copyright (c) 2026 Cisco and/or its affiliates.
# SPDX-License-Identifier: Apache-2.0
#
# Ordered release pipeline:
#   1. Build docs — validate MkDocs output
#   2. Mike — push versioned site to gh-pages (from docs/mkdocs/mike_versions.ini [versions] release)
#   3. Git tag — create v* tag on the commit that was published
#   4. Finalize — optional GitHub Release
#
# Copy to .github/workflows/ at repo root. Expects docs/ (install.sh, Taskfile.yml) and docs/mkdocs/; optional uv in docs/mkdocs/.

name: Docs release

on:
  workflow_dispatch:
    inputs:
      create_github_release:
        description: 'After tagging, create a GitHub Release for the same tag'
        type: choice
        options:
          - 'no'
          - 'yes'
        default: 'no'

permissions:
  contents: write

concurrency:
  group: docs-release
  cancel-in-progress: false

jobs:
  build:
    name: '1. Build docs'
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup Taskfile
        shell: bash
        run: sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/.local/bin

      - name: Setup UV
        shell: bash
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Update PATH
        shell: bash
        run: echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install docs site assets (install.sh)
        shell: bash
        run: |
          chmod +x docs/install.sh
          ./docs/install.sh
          ./docs/install.sh --with-mike-macros

      - name: Sync Python environment (docs/mkdocs)
        shell: bash
        run: |
          if [ -f docs/mkdocs/pyproject.toml ] || [ -f docs/mkdocs/uv.lock ]; then
            (cd docs/mkdocs && uv sync)
          else
            python3 -m pip install -r docs/requirements-agntcy-docs-theme.txt
          fi

      - name: Build
        shell: bash
        run: |
          if [ -f docs/mkdocs/uv.lock ] || [ -f docs/mkdocs/pyproject.toml ]; then
            export VERSION="$(cd docs/mkdocs && uv run python mike_version.py release)"
          else
            export VERSION="$(cd docs/mkdocs && python mike_version.py release)"
          fi
          task -t docs/Taskfile.yml build

  mike:
    name: '2. Mike (gh-pages)'
    needs: build
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup Taskfile
        shell: bash
        run: sh -c "$(curl --location https://taskfile.dev/install.sh)" -- -d -b ~/.local/bin

      - name: Setup UV
        shell: bash
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Update PATH
        shell: bash
        run: echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Install docs site assets (install.sh)
        shell: bash
        run: |
          chmod +x docs/install.sh
          ./docs/install.sh
          ./docs/install.sh --with-mike-macros

      - name: Sync Python environment (docs/mkdocs)
        shell: bash
        run: |
          if [ -f docs/mkdocs/pyproject.toml ] || [ -f docs/mkdocs/uv.lock ]; then
            (cd docs/mkdocs && uv sync)
          else
            python3 -m pip install -r docs/requirements-agntcy-docs-theme.txt
          fi

      - name: Configure Git for mike
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com

      - name: Fetch gh-pages
        continue-on-error: true
        run: git fetch origin gh-pages --depth=1

      - name: Remove retired mike versions (dropdown cleanup)
        shell: bash
        run: ./docs/mkdocs/delete-retired-mike-versions.sh --push

      - name: Mike deploy
        shell: bash
        run: task -t docs/Taskfile.yml mike:deploy-pages

  git-tag:
    name: '3. Git tag'
    needs: mike
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup UV
        shell: bash
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Update PATH
        shell: bash
        run: echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Sync Python environment (docs/mkdocs)
        shell: bash
        run: |
          if [ -f docs/mkdocs/pyproject.toml ] || [ -f docs/mkdocs/uv.lock ]; then
            (cd docs/mkdocs && uv sync)
          else
            python3 -m pip install -r docs/requirements-agntcy-docs-theme.txt
          fi

      - name: Create and push tag
        shell: bash
        env:
          TAG_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if [ -f docs/mkdocs/uv.lock ] || [ -f docs/mkdocs/pyproject.toml ]; then
            VER="$(cd docs/mkdocs && uv run python mike_version.py release)"
          else
            VER="$(cd docs/mkdocs && python mike_version.py release)"
          fi
          TAG="v${VER}"
          if git ls-remote --tags origin "refs/tags/${TAG}" | grep -q .; then
            echo "::error::Tag ${TAG} already exists on origin. Bump [versions] release in docs/mkdocs/mike_versions.ini or delete the remote tag."
            exit 1
          fi
          git tag -a "${TAG}" -m "Documentation release ${TAG}" "${TAG_SHA}"
          git push origin "${TAG}"

  finalize:
    name: '4. Deploy / release'
    needs: git-tag
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup UV
        shell: bash
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Update PATH
        shell: bash
        run: echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Sync Python environment (docs/mkdocs)
        shell: bash
        run: |
          if [ -f docs/mkdocs/pyproject.toml ] || [ -f docs/mkdocs/uv.lock ]; then
            (cd docs/mkdocs && uv sync)
          else
            python3 -m pip install -r docs/requirements-agntcy-docs-theme.txt
          fi

      - name: GitHub Release
        if: ${{ github.event.inputs.create_github_release == 'yes' }}
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if [ -f docs/mkdocs/uv.lock ] || [ -f docs/mkdocs/pyproject.toml ]; then
            VER="$(cd docs/mkdocs && uv run python mike_version.py release)"
          else
            VER="$(cd docs/mkdocs && python mike_version.py release)"
          fi
          TAG="v${VER}"
          gh release create "${TAG}" --title "Documentation ${TAG}" --notes "Published from workflow [${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}). Site is served from the gh-pages branch."

      - name: Pages note
        if: ${{ github.event.inputs.create_github_release != 'yes' }}
        run: |
          echo "::notice::GitHub Pages (if configured from gh-pages) is already serving the mike version from step 2. Set **create_github_release** to **yes** to open a GitHub Release on the new tag."
